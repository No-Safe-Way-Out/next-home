'use client'

import {createTheme} from "@mui/material";

const theme = createTheme({
    shape: {
        borderRadius: 15
    },
    palette: {
        primary: {
            main: '#F55255'
        }
    }
});

export default theme;
